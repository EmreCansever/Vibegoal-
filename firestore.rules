rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    match /users/{userId} {
      // Giris yapmis kullanicilar diger uyelerin profilini okuyabilir (oda liderlik tablosu)
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    match /rooms/{roomId} {
      // Giris yapmis herkes odalari okuyabilir (keşfet + davet kodu)
      allow read: if request.auth != null;

      // Oda olusturma: sahibi ve memberIds icinde olmali
      allow create: if request.auth != null
        && request.resource.data.ownerId == request.auth.uid
        && request.auth.uid in request.resource.data.memberIds;

      // Guncelleme: katilma, ayrilma, admin duzenleme
      allow update: if request.auth != null;

      // Silme: yalnizca oda sahibi
      allow delete: if request.auth != null
        && resource.data.ownerId == request.auth.uid;
    }
  }
}
